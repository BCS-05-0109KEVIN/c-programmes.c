// program to calculate sum of three  numbers
#include <stdio.h>
int main() {
    //initialization and declaration
    int a;
    int b;
    int c;
    int sum;
    //enter an integer variable a
    printf("\n Enter the number",a);
    scanf("%d",&a);
    //enter an integer variable b
    printf("\n Enter the number",b);
    scanf("%d",&b);
    //enter an integer variable c
    printf("\n Enter the number",c);
    scanf("%d",&c);
    //the sum corresponds to
    sum=a+b+c;
    printf("The sum is therefore:%d",sum);
    return 0;
}
